name: pipelineNel

on:
  workflow_dispatch:
    branches:
      - feature/pipeline
    inputs:
      lenguaje:
        description: 'Lenguaje de programación usado'
        required: true
        type: string

jobs:
  imprimir:
    runs-on: ubuntu-latest
    steps:
      - name: mensaje
        run: echo "Hola mundo"
